// Fill in your last.fm API key below so the site auto-fills it for anyone who
// opens the page — no need to paste it in manually every time.
//
// Get a free key at https://www.last.fm/api/account/create
//
// Heads up: this repo is public, so this key is visible to anyone who looks
// at the source. That's normally fine for last.fm — it's a rate-limiting
// token, not a secret — but if you'd rather keep it private, leave this
// blank and paste the key into the site's input field instead (it'll be
// remembered in your browser only).

window.BLINDSPIN_CONFIG = {
  lastfmApiKey: "c435eda41151ce527621224e44045f1f", // <-- paste your key between the quotes
};
